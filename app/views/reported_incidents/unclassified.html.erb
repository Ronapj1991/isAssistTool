<h1>Unclassified Incidents</h1>

<%= link_to "Export CSV", unclassified_path(format: "csv"), class: 'btn btn-primary' %>

<%= column_chart @reported_incidents.group(:reported_by).count %>

<%= render partial: "incident_format" %>